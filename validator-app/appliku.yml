build_settings:
  build_image: "dockerfile"
  container_port: 8000
  expose_web_port: true
  environment_variables:
    - name: WORKFLOW_VALIDATOR_MAX_BODY_BYTES
      value: "1048576"

services:
  web:
    command: "python3 -m uvicorn app:app --host 0.0.0.0 --port 8000"
